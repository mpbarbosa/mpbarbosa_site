# Prompt Log

**Timestamp:** 2026-03-08T19:03:19.251Z
**Persona:** test_engineer
**Model:** gpt-4.1

## Prompt

```
**Role**: You are a senior test engineer specializing in writing complete, runnable test suites that cover happy paths, edge cases, and error scenarios.

**Task**: Generate a complete, runnable test file for the source file below.

**Source file**: `src/eslint.config.js`
**Language / framework**: Jest

**Requirements**:
- Output ONLY the test file content inside a single fenced code block (```js ... ```)
- Cover: happy paths, edge cases, and error scenarios
- Use Jest idioms and conventions
- Use descriptive test names
- Do NOT include explanations outside the code block

```js
import js from '@eslint/js';
import globals from 'globals';

export default [
  js.configs.recommended,
  {
    languageOptions: {
      ecmaVersion: 2022,
      sourceType: 'module',
      globals: {
        ...globals.browser,
        ...globals.node,
        ...globals.jest,
        define: 'readonly', // AMD loader global
      },
    },
    rules: {
      'no-console': ['warn', { allow: ['warn', 'error'] }],
      'no-var': 'error',
      'prefer-const': 'error',
      'no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
      'no-undef': 'error',
      eqeqeq: ['error', 'always'],
      curly: ['error', 'all'],
      indent: ['error', 2, { SwitchCase: 1 }],
      semi: ['error', 'always'],
      quotes: ['error', 'single', { avoidEscape: true, allowTemplateLiterals: true }],
      'no-trailing-spaces': 'error',
      'comma-dangle': ['error', 'always-multiline'],
      'object-curly-spacing': ['error', 'always'],
      'array-bracket-spacing': ['error', 'never'],
      'arrow-spacing': ['error', { before: true, after: true }],
      'keyword-spacing': ['error', { before: true, after: true }],
      'space-before-blocks': ['error', 'always'],
      'space-infix-ops': 'error',
      'no-multiple-empty-lines': ['error', { max: 2, maxEOF: 1 }],
    },
  },
  {
    ignores: [
      'assets/js/*',
      'node_modules/**',
      'coverage/**',
      '**/*.min.js',
      'jest-environment-jsdom-no-warnings.cjs',
      'submodules/**',
    ],
  },
];

```

Now generate the test file:


**Approach**: Analyse only the **src/eslint.config.js** source code shown above. Do not reference any files or modules not visible in the context.

Generate a self-contained test file that:
- Imports from the source file using the correct relative path
- Uses Jest idioms and conventions
- Covers happy paths, edge cases, and error handling
- Uses descriptive `describe`/`it` (or equivalent) blocks with clear test names
- Does NOT include any explanations outside the code block

```

## Response

```
```js
// src/eslint.config.test.js
import config from './eslint.config.js';
import js from '@eslint/js';
import globals from 'globals';

describe('eslint.config.js', () => {
  it('should export an array of configs', () => {
    expect(Array.isArray(config)).toBe(true);
    expect(config.length).toBe(3);
  });

  it('should include js.configs.recommended as first element', () => {
    expect(config[0]).toBe(js.configs.recommended);
  });

  describe('languageOptions', () => {
    const langOpts = config[1].languageOptions;

    it('should set ecmaVersion to 2022', () => {
      expect(langOpts.ecmaVersion).toBe(2022);
    });

    it('should set sourceType to module', () => {
      expect(langOpts.sourceType).toBe('module');
    });

    it('should include browser, node, and jest globals', () => {
      expect(langOpts.globals).toMatchObject({
        ...globals.browser,
        ...globals.node,
        ...globals.jest,
      });
    });

    it('should set define global to readonly', () => {
      expect(langOpts.globals.define).toBe('readonly');
    });
  });

  describe('rules', () => {
    const rules = config[1].rules;

    it('should warn on console except warn/error', () => {
      expect(rules['no-console']).toEqual(['warn', { allow: ['warn', 'error'] }]);
    });

    it('should error on var usage', () => {
      expect(rules['no-var']).toBe('error');
    });

    it('should error on prefer-const', () => {
      expect(rules['prefer-const']).toBe('error');
    });

    it('should error on unused vars, ignoring args starting with _', () => {
      expect(rules['no-unused-vars']).toEqual(['error', { argsIgnorePattern: '^_' }]);
    });

    it('should error on undefined variables', () => {
      expect(rules['no-undef']).toBe('error');
    });

    it('should enforce eqeqeq always', () => {
      expect(rules.eqeqeq).toEqual(['error', 'always']);
    });

    it('should enforce curly braces for all', () => {
      expect(rules.curly).toEqual(['error', 'all']);
    });

    it('should enforce 2-space indent, SwitchCase: 1', () => {
      expect(rules.indent).toEqual(['error', 2, { SwitchCase: 1 }]);
    });

    it('should enforce semicolons', () => {
      expect(rules.semi).toEqual(['error', 'always']);
    });

    it('should enforce single quotes, allow template literals', () => {
      expect(rules.quotes).toEqual(['error', 'single', { avoidEscape: true, allowTemplateLiterals: true }]);
    });

    it('should error on trailing spaces', () => {
      expect(rules['no-trailing-spaces']).toBe('error');
    });

    it('should enforce comma-dangle always-multiline', () => {
      expect(rules['comma-dangle']).toEqual(['error', 'always-multiline']);
    });

    it('should enforce object curly spacing', () => {
      expect(rules['object-curly-spacing']).toEqual(['error', 'always']);
    });

    it('should enforce array bracket spacing', () => {
      expect(rules['array-bracket-spacing']).toEqual(['error', 'never']);
    });

    it('should enforce arrow spacing', () => {
      expect(rules['arrow-spacing']).toEqual(['error', { before: true, after: true }]);
    });

    it('should enforce keyword spacing', () => {
      expect(rules['keyword-spacing']).toEqual(['error', { before: true, after: true }]);
    });

    it('should enforce space before blocks', () => {
      expect(rules['space-before-blocks']).toEqual(['error', 'always']);
    });

    it('should enforce space in infix ops', () => {
      expect(rules['space-infix-ops']).toBe('error');
    });

    it('should enforce no multiple empty lines', () => {
      expect(rules['no-multiple-empty-lines']).toEqual(['error', { max: 2, maxEOF: 1 }]);
    });
  });

  describe('ignores', () => {
    const ignores = config[2].ignores;

    it('should ignore assets/js/*', () => {
      expect(ignores).toContain('assets/js/*');
    });

    it('should ignore node_modules/**', () => {
      expect(ignores).toContain('node_modules/**');
    });

    it('should ignore coverage/**', () => {
      expect(ignores).toContain('coverage/**');
    });

    it('should ignore all *.min.js files', () => {
      expect(ignores).toContain('**/*.min.js');
    });

    it('should ignore jest-environment-jsdom-no-warnings.cjs', () => {
      expect(ignores).toContain('jest-environment-jsdom-no-warnings.cjs');
    });

    it('should ignore submodules/**', () => {
      expect(ignores).toContain('submodules/**');
    });
  });

  describe('error scenarios', () => {
    it('should not throw if config is missing expected keys', () => {
      const badConfig = [{}, {}, {}];
      expect(() => {
        badConfig[1].languageOptions?.ecmaVersion;
        badConfig[1].rules?.['no-var'];
        badConfig[2].ignores?.includes('assets/js/*');
      }).not.toThrow();
    });

    it('should handle empty config array gracefully', () => {
      expect(() => {
        [].forEach(() => {});
      }).not.toThrow();
    });
  });
});
```
```