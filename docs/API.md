# API Reference

This project is primarily a static site, but if you add JavaScript modules or REST endpoints, document them here.

## Example: JavaScript Module

```
// src/components/Example.js
/**
 * ExampleComponent renders a greeting.
 * @param {string} name - Name to greet
 * @returns {JSX.Element}
 */
function ExampleComponent({ name }) {
  return <div>Hello, {name}!</div>;
}
```

Add more API documentation as the codebase evolves.
